{
  "name": "deployment-builder",
  "version": "1.0.0",
  "description": "Service for processing deployment requests from Redis queue",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --no-notify --exit-child src/index.ts",
    "test": "jest",
    "setup": "mkdir -p tmp/deployments",
    "clean": "rm -rf tmp/deployments/*"
  },
  "dependencies": {
    "aws-sdk": "^2.1472.0",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "ioredis": "^5.6.0",
    "simple-git": "^3.20.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.8.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
